# validation_answers_batch3.txt
# Compiler-Grade AST Complexity Validation — Batch 3 (test401–test600)
# Format: identifier -> O(...)
#
# Symbol-preservation rules strictly applied:
#   - Variable names match PARENT function parameter names exactly
#   - Container sizes written as v.size(), str.size(), etc., or as
#     the aliased name used in the parent function
#   - No normalization of distinct symbolic dimensions
#   - No collapsing distinct params into n² / n³ unless SAME symbol
# ─────────────────────────────────────────────────────────────────

# ── New helpers ──────────────────────────────────────────────────
helper61  -> O(wid)
helper62  -> O(hgt)
helper63  -> O(wid*hgt)
helper64  -> O(cap)
helper65  -> O(lim)
helper66  -> O(cap)
helper67  -> O(lim)
helper68  -> O(n*log(m))
helper69  -> O(wid*hgt)
helper70  -> O(wid*hgt)
helper71  -> O(cap+lim)
helper72  -> O(cap+lim)
helper73  -> O(n)
helper74  -> O(n)
helper75  -> O(n)
helper76  -> O(n*m)
helper77  -> O(n*m)
helper78  -> O(n*m)
helper79  -> O(n^2*log(n))
helper80  -> O(n*log(n))
helper81  -> O(wid+hgt)
helper82  -> O(wid+hgt)
helper83  -> O(cap*lim)
helper84  -> O(n)
helper85  -> O(n+m+r+t)
helper86  -> O(n*m*r*t)
helper87  -> O(n)
helper88  -> O(n)
helper89  -> O(n)
helper90  -> O(n*m)

# ── §A  Cast Handling ─────────────────────────────────────────────
# test401: v.size() cast (long long) then (int), alias n, linear loop
test401   -> O(n)
# test402: v.size() cast (size_t) then (int), alias n, linear loop
test402   -> O(v.size())
# test403: (int)v.size() directly in fo macro bound
test403   -> O(v.size())
# test404: (long long)v.size() -> foLL, n = v.size()
test404   -> O(v.size())
# test405: (int)v.size() directly in rep macro upper bound
test405   -> O(v.size())
# test406: (int)v.size() as argument to helper26 (O(p) with p=v.size())
test406   -> O(v.size())
# test407: (long long)v.size() to helper84 (long long param, O(n))
test407   -> O(v.size())
# test408: (int)(long long)str.size(), alias n, loop
test408   -> O(str.size())
# test409: two (long long)-cast sizes -> n, m, sequential loops
test409   -> O(dq.size() + v.size())
# test410: two (size_t)-cast sizes -> n, m, nested loops
test410   -> O(u.size()v.size())
# test411: (long long)-cast size -> alias n -> helper50 (4-deep O(n))
test411   -> O(v.size())
# test412: (size_t)-cast size -> n -> 2-hop alias y -> loop
test412   -> O(v.size())
# test413: (size_t) casts, two sets -> n, m -> helper30 (O(n*m))
test413   -> O(ms.size()st.size())
# test414: FORI(i, 0, (int)v.size()) macro
test414   -> O(v.size())
# test415: (int)str.length() directly in loop bound
test415   -> O(str.length())
# test416: (int)str.size() directly in loop bound
test416   -> O(str.size())
# test417: n param, (long long) cast size alias m, sequential
test417   -> O(n+m)
# test418: n param, (size_t) cast size alias m, nested
test418   -> O(n*m)
# test419: array<int,100>.size() cast to int n, loop
test419   -> O(arr.size())
# test420: array.size() n, vector.size() m, sequential loops
test420   -> O(arr.size() + v.size())

# ── §B  Parenthesized Expressions ────────────────────────────────
# test421: (((n))) triple-paren directly in for bound
test421   -> O(n)
# test422: alias x = (((n))), loop on x
test422   -> O(n)
# test423: fo(i, (n)) single-paren macro arg
test423   -> O(n)
# test424: fo(i, ((n))) double-paren macro arg
test424   -> O(n)
# test425: rep(i, 0, (n)) paren upper macro arg
test425   -> O(n)
# test426: (int)((v.size())) double-paren cast alias n
test426   -> O(v.size())
# test427: fo(i, ((int)v.size())) paren-wrapped cast in macro
test427   -> O(v.size())
# test428: total = ((n+m)) double-paren compound alias, loop
test428   -> O(m + n)
# test429: fo(i, ((total))) where total = n+m
test429   -> O(m + n)
# test430: b = (((a))) where a = n, loop on b
test430   -> O(n)
# test431: while(i < (((n)))) triple-paren while bound
test431   -> O(n)
# test432: x = (int)((n)), loop on x
test432   -> O(n)
# test433: ((n)) in both nested loop bounds
test433   -> O(n²)
# test434: (n) and (m) single-paren nested bounds
test434   -> O(nm)

# ── §C  Triple-Nested Macros ─────────────────────────────────────
# test435: fo(fo(fo(n,n,n))) same param
test435   -> O(n^3)
# test436: fo(fo(fo(n,m,r))) three distinct params
test436   -> O(n*m*r)
# test437: fo(fo(fo(n,n,m))) two n, one m
test437   -> O(n^2*m)
# test438: fo(fo(fo(n,m,m))) one n, two m
test438   -> O(n*m^2)
# test439: rep(rep(rep(n,n,n)))
test439   -> O(n^3)
# test440: rep(rep(rep(n,m,r)))
test440   -> O(n*m*r)
# test441: fo/fod/rep three levels, same n
test441   -> O(n^3)
# test442: fo/rep/fod three levels, distinct n,m,r
test442   -> O(n*m*r)
# test443: NEST3 macro, same n three times
test443   -> O(n^3)
# test444: NEST3 macro, n*m*r
test444   -> O(n*m*r)
# test445: NEST2 macro, n*m
test445   -> O(n*m)
# test446: NEST2 macro, n^2
test446   -> O(n^2)
# test447: fo(fo(fo(n,m,1))) — innermost is 1 => O(n*m)
test447   -> O(n*m)
# test448: fo four levels: n,n,m,1 => O(n^2*m)
test448   -> O(n^2*m)

# ── §D  Alias→Cast→Container→Macro chains ─────────────────────
# test449: v.size()->(int)->alias->fo — O(n)
test449   -> O(v.size())
# test450: v.size()->(long long)->(int)->x->y->fo — O(n)
test450   -> O(v.size())
# test451: v.size()->cast->alias->fo(fo) — O(n^2)
test451   -> O(n^2)
# test452: two cast aliases -> nested fo macros — O(n*m)
test452   -> O(n*m)
# test453: str.size()->(int)->x->rep — O(n)
test453   -> O(str.size())
# test454: v.size()->a, str.size()->b, fo a then fo b — O(n+m)
test454   -> O(str.size() + v.size())
# test455: map.size()->(long long)->(int)->helper64 — O(n)
test455   -> O(mp.size())
# test456: ump.size()->(size_t)->(int)->helper73 — O(n)
test456   -> O(mp.size())
# test457: v.size()->cast->a->b->c->helper75 — O(n)
test457   -> O(v.size())
# test458: two (long long)-cast sizes -> helper30 — O(n*m)
test458   -> O(u.size()v.size())
# test459: v.size()->cast->vsz->n, total=n+extra -> fo — O(n+extra)
test459   -> O(extra + v.size())
# test460: v.size()->(long long)->n, helper30(n,m) — O(n*m)
test460   -> O(n*m)
# test461: (int)(((v.size()))) nested cast+paren alias, loop — O(n)
test461   -> O(v.size())
# test462: (int)((v.size())) and (int)((dq.size())), sequential — O(n+m)
test462   -> O(dq.size() + v.size())

# ── §E  Symbolic Expressions ─────────────────────────────────────
# test463: loop bound inline n+m (no alias)
test463   -> O(m + n)
# test464: while bound inline n+m
test464   -> O(m + n)
# test465: inline three-term n+m+r
test465   -> O(m + n + r)
# test466: inline n+(int)v.size()
test466   -> O(n+m)
# test467: fo(i, total) where total=a+b
test467   -> O(a+b)
# test468: helper29(a,b) with a=n, b=m aliased
test468   -> O(m + n)
# test469: helper39(x,y,z) with x=a,y=b,z=c
test469   -> O(a+b+c)
# test470: inline wid+hgt loop bound
test470   -> O(hgt + wid)
# test471: helper63(wid,hgt) — O(wid*hgt)
test471   -> O(wid*hgt)
# test472: helper71(cap,lim) — O(cap+lim)
test472   -> O(cap+lim)
# test473: helper83(cap,lim) — O(cap*lim)
test473   -> O(caplim)
# test474: n+vsz alias total -> fo — O(n+v.size())
test474   -> O(n+v.size())
# test475: helper85(a,b,c,0) with a=n,b=m,c=r — O(n+m+r)
test475   -> O(m + n + r)
# test476: helper85(n,m,r,t) — O(n+m+r+t)
test476   -> O(m + n + r + t)
# test477: helper86(n,m,r,t) — O(n*m*r*t)
test477   -> O(nmrt)

# ── §F  Amortized / Special Loop Patterns ────────────────────────
# test478: i+=2, bound n — O(n)
test478   -> O(n)
# test479: i+=3, bound n — O(n)
test479   -> O(n)
# test480: i+=k, bound n — O(n)
test480   -> O(n)
# test481: loop with continue, bound n — O(n)
test481   -> O(n)
# test482: loop with unreachable break, bound n — O(n)
test482   -> O(n)
# test483: trial division for(i=2; i*i<=n; i++) — O(sqrt(n))
test483   -> O(sqrt(n))
# test484: trial division while form — O(sqrt(n))
test484   -> O(sqrt(n))
# test485: i*i<=lim where lim=n — O(sqrt(n))
test485   -> O(sqrt(n))
# test486: two-pointer converge on n — O(n)
test486   -> O(n)
# test487: two-pointer on vector of size n — O(n)
test487   -> O(n)
# test488: sliding window, r advances n times total — O(n)
test488   -> O(n)
# test489: sliding window on vector of size n — O(n)
test489   -> O(n)
# test490: step=1 constant, while i<n — O(n)
test490   -> O(n)

# ── §G  Scope / Declaration Edge Cases ───────────────────────────
# test491: alias declared after inner block, lim=n — O(n)
test491   -> O(n)
# test492: bound assigned in if-branch = m — O(m)
test492   -> O(m)
# test493: for-init int lim=n, loop on lim — O(n)
test493   -> O(n)
# test494: for-init int lim=m, loop on lim — O(m)
test494   -> O(m)
# test495: for-init int n=(int)v.size(), loop — O(n)
test495   -> O(v.size())
# test496: alias n2=n in for-init lim=n2 — O(n)
test496   -> O(n)
# test497: both branches set lim=n — O(n)
test497   -> O(n)
# test498: r,c assigned in block then used in nested loops — O(rows*cols)
test498   -> O(rows*cols)
# test499: inner block per iteration is O(1) — O(n)
test499   -> O(n)
# test500: b=a=n in inner block, loop on b — O(n)
test500   -> O(n)

# ── §H  Multi-Container Size Combinations ────────────────────────
# test501: three containers n,m,r sequential — O(n+m+r)
test501   -> O(n+m+r)
# test502: three container sizes n*m*r nested — O(n*m*r)
test502   -> O(n*m*r)
# test503: same v.size() aliased twice n1,n2, nested — O(n^2)
test503   -> O(v.size()²)
# test504: map+ump sizes n,m sequential — O(n+m)
test504   -> O(mp.size() + ump.size())
# test505: map+ump sizes n,m nested — O(n*m)
test505   -> O(mp.size()ump.size())
# test506: multimap.size() alias n, rep loop — O(n)
test506   -> O(mm.size())
# test507: helper85(n,m,r,0) with container sources — O(n+m+r)
test507   -> O(n+m+r)
# test508: s1.size()+s2.size() summed total, loop — O(n+m)
test508   -> O(s1.size() + s2.size())
# test509: v.size() aliased three times n3=n2=n1, loop on n3 — O(n)
test509   -> O(v.size())
# test510: v.size() 2-hop n, w.size() 2-hop m, nested — O(n*m)
test510   -> O(v.size()w.size())

# ── §I  Spatial/Capacity Naming ──────────────────────────────────
# test511: helper61(wid) — O(wid)
test511   -> O(wid)
# test512: helper62(hgt) — O(hgt)
test512   -> O(hgt)
# test513: helper63(wid,hgt) — O(wid*hgt)
test513   -> O(wid*hgt)
# test514: helper70(wid,hgt) 3-deep chain — O(wid*hgt)
test514   -> O(wid*hgt)
# test515: helper82(wid,hgt) — O(wid+hgt)
test515   -> O(hgt + wid)
# test516: helper66(cap) — O(cap)
test516   -> O(cap)
# test517: helper67(lim) — O(lim)
test517   -> O(lim)
# test518: helper72(cap,lim) — O(cap+lim)
test518   -> O(cap+lim)
# test519: helper83(c,l) where c=cap,l=lim — O(cap*lim)
test519   -> O(caplim)
# test520: helper63(wid,hgt) with wid=v.size(),hgt=u.size() — O(wid*hgt)
test520   -> O(wid*hgt)
# test521: wid=(int)v.size(), helper61(wid) — O(wid)
test521   -> O(wid)
# test522: helper75(wid) — O(wid)
test522   -> O(wid)
# test523: helper89(cap) — O(cap)
test523   -> O(cap)

# ── §J  Log-Linear with Two Distinct Params ──────────────────────
# test524: helper68(n,m) — O(n*log(m))
test524   -> O(n*log(m))
# test525: helper68(rows,cols) — O(rows*log(cols))
test525   -> O(rows*log(cols))
# test526: helper68(a,b) — O(a*log(b))
test526   -> O(a*log(b))
# test527: helper80(n) = helper68(n,n) — O(n*log(n))
test527   -> O(n*log(n))
# test528: outer n, inner while j>0; j>>=1 on m — O(n*log(m))
test528   -> O(n*log(m))
# test529: outer fo on n, inner /=2 on m — O(n*log(m))
test529   -> O(n*log(m))
# test530: outer rows, inner <<= on cols — O(rows*log(cols))
test530   -> O(rows*log(cols))
# test531: outer n=v.size(), inner /=2 on m — O(n*log(m))
test531   -> O(n*log(m))
# test532: helper79(n) — O(n^2*log(n))
test532   -> O(n^2*log(n))

# ── §K  Macro-Body Helper Chains ─────────────────────────────────
# test533: helper73(n) — O(n)
test533   -> O(n)
# test534: helper74(n) — O(n)
test534   -> O(n)
# test535: helper75(n) — O(n)
test535   -> O(n)
# test536: helper75(m) — O(m)
test536   -> O(m)
# test537: helper75(k) — O(k)
test537   -> O(k)
# test538: helper89(n) — O(n)
test538   -> O(n)
# test539: helper89(m) — O(m)
test539   -> O(m)
# test540: helper90(n,m) — O(n*m)
test540   -> O(n*m)
# test541: helper78(n,m) — O(n*m)
test541   -> O(n*m)
# test542: helper78(a,b) — O(a*b)
test542   -> O(a*b)
# test543: helper78(rows,cols) — O(rows*cols)
test543   -> O(rows*cols)
# test544: v.size()->cast->n->helper75 — O(n)
test544   -> O(v.size())
# test545: v.size()->cast->n, helper90(n,m) — O(n*m)
test545   -> O(n*m)

# ── §L  Deep Cross-Concept Chains ────────────────────────────────
# test546: v.size()->cast->n->n2->n3->helper75 — O(n)
test546   -> O(v.size())
# test547: map.size()->(long long)->(int)->alias->rep — O(n)
test547   -> O(mp.size())
# test548: two cast aliases -> fo(fo) — O(n*m)
test548   -> O(n*m)
# test549: (size_t)->(int) cast -> n -> fo(((n))) fo(((n))) — O(n^2)
test549   -> O(n^2)
# test550: v.size()->n, str.size()->m, total=n+m, fo — O(n+m)
test550   -> O(str.size() + v.size())
# test551: v.size()->wid, u.size()->hgt, helper70 — O(wid*hgt)
test551   -> O(wid*hgt)
# test552: three containers->helper39(n,m,r) — O(n+m+r)
test552   -> O(n+m+r)
# test553: two container sizes->helper68(n,m) — O(n*log(m))
test553   -> O(n*log(m))
# test554: v.size()->(long long)->(int)->n->a->b->helper50 — O(n)
test554   -> O(v.size())
# test555: NEST2((int)v.size(),(int)u.size()) — O(n*m)
test555   -> O(n*m)
# test556: NEST3((((n))),(((n))),(((n)))) — O(n^3)
test556   -> O(n^3)
# test557: b=v.size(), fo(b)*rep(m) — O(n*m)
test557   -> O(n*m)
# test558: n=(int)v.size(), total=n+m, fo — O(n+m)
test558   -> O(m + v.size())

# ── §M  New Loop Structures ──────────────────────────────────────
# test559: n=str.size()-1, repe(0..n) = str.size() iters — O(n)
test559   -> O(n)
# test560: repe(0, v.size()-1) = v.size() iters — O(n)
test560   -> O(n)
# test561: half=n/2, loop on half — O(n)
test561   -> O(n)
# test562: fod(i,n) — O(n)
test562   -> O(n)
# test563: loop with multiple continues, bound n — O(n)
test563   -> O(n)
# test564: outer n, inner 2*(m/2) total = m — O(n*m)
test564   -> O(nm)
# test565: i=i*2 (same as *=2), bound n — O(log(n))
test565   -> O(log(n))
# test566: loop n, O(1) body — O(n)
test566   -> O(n)
# test567: outer n, inner m — O(n*m)
test567   -> O(nm)
# test568: triangle pattern j=i..n-1 — O(n^2)
test568   -> O(n²)
# test569: strict upper triangle j=i+1..n-1 — O(n^2)
test569   -> O(n²)

# ── §N  Additional Function Reuse ────────────────────────────────
# test570: helper63(rows,cols) — O(rows*cols)
test570   -> O(rows*cols)
# test571: helper63(n,m) with container-derived sizes — O(n*m)
test571   -> O(u.size()v.size())
# test572: helper71(n,m) — O(n+m)
test572   -> O(m + n)
# test573: helper83(n,m) — O(n*m)
test573   -> O(nm)
# test574: v.size()->n, helper90(n,m) — O(n*m)
test574   -> O(n*m)
# test575: helper70(n,m) with params n,m — O(n*m)
test575   -> O(nm)

# ── §O  Combined Alias+Scope+Cast Stress ─────────────────────────
# test576: v.size()->(ll)->(int)->n->scope: p=n->fo — O(n)
test576   -> O(v.size())
# test577: cast alias n, inner shadow n=1, outer loop uses outer n — O(n)
test577   -> O(v.size())
# test578: outer lim=n, inner shadow lim=m, a=lim=m, fo(a) — O(m)
test578   -> O(m)
# test579: n=v.size(), inner mm=m, fo(n)*fo(mm) — O(n*m)
test579   -> O(n*m)
# test580: for-init n=(int)v.size(), loop — O(n)
test580   -> O(v.size())
# test581: for-init n=v.size(), inner loop to n — O(n^2)
test581   -> O(v.size()²)

# ── §P  Rare Structural Forms ────────────────────────────────────
# test582: long long alias, foLL macro — O(n)
test582   -> O(n)
# test583: repLL nested with long long bounds — O(n*m)
test583   -> O(n*m)
# test584: (int)str.size() directly in for bound — O(n)
test584   -> O(str.size())
# test585: v.size() as unsigned, size_t loop — O(n)
test585   -> O(v.size())
# test586: size_t loops over two containers — O(n+m)
test586   -> O(dq.size() + v.size())
# test587: size_t nested loops, same container — O(n^2)
test587   -> O(v.size()²)
# test588: ptrdiff_t alias, loop — O(n)
test588   -> O(v.size())
# test589: alias mm=m, outer n, inner mm — O(n*m)
test589   -> O(nm)
# test590: outer n, inner helper43(n) per iter — O(n*log(n))
test590   -> O(n*log(n))
# test591: outer fo(n), inner helper28(n) per iter — O(n*log(n))
test591   -> O(n*log(n))
# test592: outer n*m, inner log r via >>= — O(n*m*log(r))
test592   -> O(n*m*log(r))
# test593: nn=n, mm=m, fo(nn)*inner-log(mm) — O(n*log(m))
test593   -> O(n*log(m))
# test594: outer loop n, inner O(1) per iter — O(n)
test594   -> O(n)
# test595: for-init b=a=n, loop on b — O(n)
test595   -> O(n)
# test596: two for-inits: lim=n then lim=m, sequential — O(n+m)
test596   -> O(m + n)
# test597: g.size()->rows, g[0].size()->cols, helper63 — O(rows*cols)
test597   -> O(rows*cols)
# test598: v.size()->n, u.size()->m, total=n+m, while — O(n+m)
test598   -> O(u.size() + v.size())
# test599: two (long long) cast aliases 2-hop each, NEST2 — O(n*m)
test599   -> O(n*m)
# test600: v.size()->cast->n, fo(fo)*helper34(n) — O(n^2*log(n))
test600   -> O(n^2*log(n))
